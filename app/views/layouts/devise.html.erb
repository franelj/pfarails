<!DOCTYPE html>
<html>
<head>
  <title>Spot&Snap</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <!-- Favicons -->
  <link rel="shortcut icon" href="<%= asset_path "icons/favicon.ico" %>">
  <link rel="apple-touch-icon" sizes="144x144" href="<%= asset_path "icons/apple-touch-icon-144x144.png" %>">
  <link rel="apple-touch-icon" sizes="114x114" href="<%= asset_path "icons/apple-touch-icon-114x114.png" %>">
  <link rel="apple-touch-icon" sizes="72x72" href="<%= asset_path "icons/apple-touch-icon-72x72.png" %>">
  <link rel="apple-touch-icon" href="<%= asset_path "icons/apple-touch-icon.png" %>">

  <!-- Google Fonts -->
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,300italic,800italic,800,700italic,700,600italic,600,400italic,300' rel='stylesheet' type='text/css'>

  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
  <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  <![endif]-->
</head>
<body>
<div class="container">
  <% if flash[:notice] %>
      <div class="alert alert-info" role="alert"><%= flash[:notice] %></div>
  <% end %>
  <div class="row">
    <div class="col-sm-12 titlebar">
      <%= image_tag "logo-small.png", alt: "Spot&Snap" %>
      <br><br>
      <h2>Spot&Snap</h2>
      <p><%= content_for?(:page_title) ? yield(:page_title) : "" %></p>
    </div>
  </div>
  <div class="row">
    <%= yield %>
  </div>
</div>
</body>
</html>
